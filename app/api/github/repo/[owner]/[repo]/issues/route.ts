import {NextRequest,NextResponse} from 'next/server';import{gh}from'@/lib/github';export async function GET(req:NextRequest,{params}:{params:Promise<{owner:string;repo:string}>}){try{const{owner,repo}=await params;const state=req.nextUrl.searchParams.get('state')||'open';return NextResponse.json({items:await gh(`/repos/${owner}/${repo}/issues?state=${state}&per_page=50`)})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
